// ? ========================= DOM 
/**
 * *DOM (Document Object Model), representasi struktur HTML atau XMLdalam bentuk tree (pohon), yang memungkinkan program
 * *-> mengakses dan memanipulasi konten, struktur, dan gaya halaman secara dinamis
 * *$ NODE: setiap elemen tree DOM disebut "node"
 * *$ Root Node: dokumen HTML dimulai dari node akar (document), yang merupakan entry point untuk mengakses semua
 * *--> elemen lain di halaman.
 */

console.log(document.querySelectorAll("li"));

// ? Mengakses DOM
/**
 * *getElementById(), mengakses elemen berdasarkan ID
 * *getElementsByClassName(), berdasarkan nama class
 * *getElementsByTagNama(), berdasarkan tag
 * *querySelector(), menggunakan selector css
 * *querySelectorAll(), semua elemen yang sesuai selector
 */

// ? =========================
// ? =========================
// ? =========================
// ? =========================
// ? =========================
// ? =========================