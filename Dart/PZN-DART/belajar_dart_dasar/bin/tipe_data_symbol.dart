void main() {
  /**
   * NOTE - Tipe Data Simbol
   * ? merupakan tipe data yang digunakan
   * ? bisa digunakan sebagai constant
   * ? dibuat menggunakan #, untuk kata mengandung spasi
   * ? -> gunakan Symbol("nama variabel") untuk membuat symbol
   */

  Symbol namaSaya = Symbol('Aditya Rizky');
  var symbol = #belajar_dart;

  print(namaSaya);
  print(symbol);
}
