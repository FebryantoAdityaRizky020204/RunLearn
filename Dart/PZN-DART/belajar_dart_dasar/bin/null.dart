void main() {
  /**
   * NOTE - merupakan representasi tipe data kosong
   * * secara default jika ingin menggunakan variabel harus terdapat data pada variabel tersebut,
   * * -> tetapi jika ingin membuat sebuah variabel bernilai null, dapat memberikan tanda tanya (?)
   * * -> setelah tipeData sebuah variabel
   */

  // ignore: unused_local_variable
  String namaDepan;
  String? namaLengkap;
  // print(namaDepan); // ? ini akan error karena namaDepan belum selesai dideklarasikan

  print(namaLengkap);
}
